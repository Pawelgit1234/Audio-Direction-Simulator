#pragma once

namespace ads
{
	namespace settings
	{
		const unsigned short SPEAKER_MOVEMENT_ZONE_WIDTH = 1280;
		const unsigned short SPEAKER_MOVEMENT_ZONE_HEIGHT = 720;
		const unsigned short SEQUENSER_ZONE_WIDTH = 1280;
		const unsigned short SEQUENSER_ZONE_HEIGHT = 300;

		const unsigned short NORMAL_SOUND_LEVEL = 50;

		const float MIN_ZOOM = 2.f;
		const float MAX_ZOOM = 10.f;

		const float OBJECT_SIZE = 50.f;

		extern const char* WINDOW_NAME;
		extern const char* ICON_PATH;
	}
}